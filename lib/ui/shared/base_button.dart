import 'package:flutter/material.dart';

abstract class BaseButton {
  bool? disabled;
  double? radius;
  String? buttonText;
  Color? fontColor;
  Color? mainColor;
  Color? borderColor;
  IconData? icon;
  bool? rounded;
}
